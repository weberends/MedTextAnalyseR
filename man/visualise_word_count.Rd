% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualise_word_count.R
\name{visualise_word_count}
\alias{visualise_word_count}
\title{Visualise word counts}
\usage{
visualise_word_count(
  df,
  top_n = 10,
  title = "Top Words",
  x_label = "Word",
  y_label = "Count",
  palette = "viridis",
  by_category = NULL
)
}
\arguments{
\item{df}{A dataframe with either a `SOEP_clean` column (raw cleaned text) or `word` and `n` columns (already tokenised data).}

\item{top_n}{Integer. The number of top words to display per group. Default is 10.}

\item{title}{Character. The title of the plot. Default is "Top Words".}

\item{x_label}{Character. The label for the x-axis. Default is "Word".}

\item{y_label}{Character. The label for the y-axis. Default is "Count".}

\item{palette}{Character. The name of the viridis color palette to use ("viridis", "magma", "plasma", "inferno", "cividis", etc.). Default is "viridis".}

\item{by_category}{Optional. Name of a column (character) to group and facet by. Default is NULL (no grouping).}
}
\value{
A ggplot2 object showing the word counts.
}
\description{
This function visualises word counts from a dataframe, either directly from raw text (SOEP_clean) or from already tokenised data (word counts).
Optionally, words can be grouped and faceted by a category such as gender, age group, etc.
The plot uses a viridis color palette for better readability.
}
\details{
If a `SOEP_clean` column is found, the function tokenises the text into individual words.
If a `word` column is found instead, it assumes the dataframe is already tokenised.
Words are counted, the top `n` words are selected, and optionally grouped and faceted by a category.
}
\examples{
# Without preprocessing (directly from SOEP_clean):
visualise_word_count(example_soep_texts)

# With preprocessing:
result <- preprocess_text(example_soep_texts)
visualise_word_count(result)

# Grouped and faceted by gender, with magma color palette:
visualise_word_count(result, by_category = "gender", palette = "magma")

}
